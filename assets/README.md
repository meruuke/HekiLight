# Assets

This folder contains project assets that are **not included** in the addon package.

| File | Used where |
|---|---|
| `logo.png` | Uploaded manually to the CurseForge project page (400×400 px minimum) |

> **Note:** The `.pkgmeta` file is configured to exclude this folder from the CurseForge zip.
